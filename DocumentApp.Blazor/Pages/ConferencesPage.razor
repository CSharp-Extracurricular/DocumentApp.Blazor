@page "/conference"

@using System.Net.Http
@inject HttpClient Http
@using System.Net.Http.Json
@using System.Threading.Tasks
@using DocumentApp.DTO

<select>
    @foreach (var item in ArrConf)
    {
        <option>@item</option>
    }
</select>

@code {
    string[] ArrConf = new string[5] { "Monography", "Textbook", "Article", "Report", "Thesis" };
}
